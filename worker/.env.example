# Copy to worker/.env and fill in. worker/.env is gitignored.
#
# These identify resources inside a Cloudflare account. They are not
# credentials, but they are account-specific, so they stay out of the repo.
D1_DATABASE_ID=
# Only needed if your wrangler login covers more than one account.
CLOUDFLARE_ACCOUNT_ID=
