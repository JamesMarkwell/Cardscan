{
  "name": "cardscan-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run config && wrangler dev --config wrangler.generated.toml",
    "deploy": "npm run config && wrangler deploy --config wrangler.generated.toml",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "migrate:local": "npm run config && wrangler d1 migrations apply cardscan --local --config wrangler.generated.toml",
    "migrate:remote": "npm run config && wrangler d1 migrations apply cardscan --remote --config wrangler.generated.toml",
    "config": "./scripts/config.sh"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "@types/node": "^26.6.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  },
  "engines": {
    "node": ">=22.5"
  }
}
